# Copy to .env and fill in. .env is gitignored.

# HuggingFace — get from https://huggingface.co/settings/tokens (write scope for push)
HF_TOKEN=

# LLM-as-judge for eval — choose one or both
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional: Weights & Biases for training run logging
WANDB_API_KEY=
