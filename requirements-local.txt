# Local CPU-only deps: data prep + eval + inference of small models.
# Training deps live in requirements-train.txt and run on Colab.

# Core
datasets>=2.20.0
huggingface_hub>=0.24.0
transformers>=4.45.0
tokenizers>=0.20.0
sentencepiece>=0.2.0
accelerate>=0.34.0

# CPU-only torch (Windows wheel; CUDA torch is installed on Colab)
torch>=2.4.0

# Data prep
pandas>=2.2.0
pyarrow>=17.0.0
tqdm>=4.66.0
datasketch>=1.6.5          # MinHash dedupe

# Eval metrics
evaluate>=0.4.2
sacrebleu>=2.4.0
rouge-score>=0.1.2
nltk>=3.9.0

# LLM-as-judge
openai>=1.40.0

# Dev
pytest>=8.3.0
ruff>=0.6.0
python-dotenv>=1.0.1
