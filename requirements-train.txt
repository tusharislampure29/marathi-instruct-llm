# Colab/Kaggle GPU-only training deps. Do NOT install locally on the Iris Xe machine.
# The Colab notebook installs these in its first cell.

# Core training stack — pinned to versions known-good as of 2026-05.
torch>=2.4.0
transformers>=4.45.0
datasets>=2.20.0
accelerate>=0.34.0
peft>=0.13.0
trl>=0.11.0
bitsandbytes>=0.43.0

# Logging
wandb>=0.18.0

# Hub upload
huggingface_hub>=0.24.0

# Misc
sentencepiece>=0.2.0
einops>=0.8.0
pyyaml>=6.0
